/**
 * Product capabilities that are intentionally unavailable in the real-payment
 * mini-program. Keep unfinished commerce features fail-closed until both their
 * payment and durable entitlement storage are ready.
 */
export const MEMBER_CARD_ENABLED = false as const;
