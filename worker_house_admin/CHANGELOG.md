# CHANGELOG

## 2026-04-26

### 后台能力扩展
- 统一活动、档案、报名、次卡订单与次卡使用记录类型定义，补充 `cover` / `covers` / `cardEligible` 字段并兼容旧活动字段。
- 升级活动编辑页：封面从单图改为可排序的多图录入，保存时自动回填首图到 `cover` / `coverImage`，新增“是否可用社畜次卡抵扣”开关。
- 扩展活动列表展示逻辑，兼容首图展示与次卡抵扣状态展示。
- 新增后台页面与菜单：`报名管理`、`次卡订单`、`档案库`，并补齐对应路由。
- 新增 MSW mock 数据与接口：活动详情/新增/编辑兼容多图和次卡字段，新增报名管理、次卡订单、档案库接口。
- 调整 mock 数据关联关系，使活动、档案、报名、次卡订单之间具备真实映射，便于联调和演示。
